SURFACE: built Go CLI sync missing config
INVOCATION: .omo/evidence/obsidian-sync-agent sync --config .omo/evidence/global-review-missing-config.json
flag provided but not defined: -config
Usage of sync:
  --vault-root string
    	local vault directory
  --vault-id string
    	server vault id
  --server string
    	server base url
  --device-id string
    	override the device id
  --verbose
    	enable debug logging
  --dry-run
    	print planned actions without writing or pushing
  --require-obsidian-refresh
    	exit non-zero if the Obsidian refresh step fails
EXIT_CODE: 2
